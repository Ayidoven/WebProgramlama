@model IEnumerable<WebP.Models.Calisan>

@{
    ViewData["Title"] = "Çalışanlar";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a href="@Url.Action("Create", "CalisanApi")" class="btn btn-primary">Yeni Çalışan Ekle</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Ad Soyad</th>
            <th>Uzmanlık Alanı</th>
            <th>Uygunluk Saatleri</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var calisan in Model)
        {
            <tr>
                <td>@calisan.Calisanid</td>
                <td>@calisan.adsoyad</td>
                <td>@calisan.uzmanlıkalanı</td>
                <td>@calisan.uygunluksaatleri</td>
                <td>
                    <a href="@Url.Action("Edit", "CalisanApi", new { id = calisan.Calisanid })" class="btn btn-warning">Güncelle</a>
                    <a href="@Url.Action("Delete", "CalisanApi", new { id = calisan.Calisanid })" class="btn btn-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>
